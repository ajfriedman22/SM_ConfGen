name: sm_confgen
channels:

  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python=3.11.5
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  #Conda specific packages
  - openff-toolkit
  - numpy

    # Pip-only installs
  - pip:
    - mpi4py
    - argparse
    - pyyaml
    - seaborn
    - matplotlib

