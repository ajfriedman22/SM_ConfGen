name: sm_confgen
channels:

  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - openff-toolkit
  - numpy

    # Pip-only installs
  - pip:
    - mpi4py
    - argparse
    - pyyaml
    - seaborn
    - matplotlib

