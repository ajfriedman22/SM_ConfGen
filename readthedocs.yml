# readthedocs.yml

version: 2

sphinx:
  # Path to your Sphinx configuration file.
  configuration: docs/conf.py
  
build:
  os: "ubuntu-22.04"
  tools:
    python: "3.8"
  image: latest

python:
  version: 3.8
  install:
    - method: pip
      path: .

conda:
  environment: env.yaml